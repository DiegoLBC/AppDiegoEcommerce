<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ads5.appdiegoecommerce">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppDiegoEcommerce">
        <activity android:name=".LoginUsuario"></activity>
        <activity android:name=".RegistroUsuarios" />
        <activity
            android:name=".InfoProduto"
            android:label="@string/title_activity_info_produto" />
        <activity android:name=".TelaAberturaActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>